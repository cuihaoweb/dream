{
    "compilerOptions": {
        // 允许从没有设置默认导出的模块中默认导入
        "allowSyntheticDefaultImports": true,
        // 允许编译javascript文件
        "allowJs": true,
        // 采用的模块系统
        "sourceMap": true,
        "target": "esnext",
        "module": "esnext",
        // 这样就可以对 `this` 上的数据属性进行更严格的推断
        "strict": true,
        "jsx": "preserve",
        "moduleResolution": "node"
    },
    "exclude": [
        "node_modules",
        "**/*.spec.ts"
    ]
}